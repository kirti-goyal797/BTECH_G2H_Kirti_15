#include <stdio.h>

int main() {
    float s = 0.0;
    int n,i;
    printf("enter series:\n");
    scanf("%d",&n);
    for( i = 1;i<=n; i++) {
    s = s+(1.0/i);
    printf("the sum of the series is: %f",s);
    }
    
    return 0;
}
