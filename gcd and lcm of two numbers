#include <stdio.h>

int main() {
    int num1, num2,p;
    printf("enter two elements:");
    scanf("%d%d",  &num1, &num2);
    p= num1*num2;
    while(num1>0 && num2>0)
    {
        if (num1>num2)
        num1 = num1 - num2;
        else
        num2 = num2 - num1;
    }
    if (num1==0)
    {
        printf("the gcd of 2 numbers is : %d \n", num2);
        printf("Lcm of two numbers is %d", p/num2);
    }else{
        printf("the gcd of two numbers is %d \n",num1);
        printf("lcm of two numbers is %d", p/num1);
    }
    return 0;
}

    
    
    
