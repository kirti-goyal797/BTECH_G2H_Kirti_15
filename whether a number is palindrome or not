#include <stdio.h>


int main ()
{
    int n;
    int rev=0;
    printf("enter number\n");
    scanf("%d",&n);
    int r ;
    int num=n;
    while(n!=0)
    {
        r=n%10;
        rev=rev*10+r;
        n=n/10;
        
    }
    if(rev==num)
    printf("it is palindrome");
    else 
    
    printf("it is not palindrome");
}
