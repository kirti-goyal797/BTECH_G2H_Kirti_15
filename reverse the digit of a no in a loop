#include <stdio.h>
int main ()
{
    int n;
    printf("enter number\n");
    scanf("%d",&n);
    int r , i;
    int rev=0;
    while(n!=0)
    {
        r=n%10;
        rev= rev*10+r;
        n=n/10;
        
    }
    printf("reversed number is %d \n", rev);
}
