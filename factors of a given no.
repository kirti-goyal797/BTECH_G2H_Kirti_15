#include <stdio.h>


int main ()
{
    int n, i;
    printf("enter number\n");
    scanf("%d",&n);
    printf("factors of this number are\n");
    for(i=1;i<=n;i++)
    {
        if(n%i==0)
        {
            printf("%d\n", i);
        }
    }
}
