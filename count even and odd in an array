#include <stdio.h>

int main() {
   int i, n;
    int c=0;
    int f=0;
    printf("enter the size of an array:");
    scanf("%d",&n);
    int a[n];
    printf("enter array elements:");
    for(i=0;i<n; i++)
    {
        scanf("%d" ,&a[i]);
    }
    for(i=0;i<n; i++)
    {
     if(a[i]%2==0)
    c++;
     else
    f++;
    }
    printf("number of elements even in an array is:%d\n",c);
   printf("number of elements odd in an array is:%d\n",f);
    

}
